<%= form_with(model: [ @test, @test.questions.build ],
              local: true) do |form| %>
  <%= render 'shared/errors', resourse: @question %>

  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
