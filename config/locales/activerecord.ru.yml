ru:
  activerecord:
    models:
      test: "Тест"
    attributes:
      test:
        title: "Название"
    errors:
      messages:
        blank: "не может быть пустым"
        is_not_valid: "не корректный"
